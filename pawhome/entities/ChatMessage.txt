{
  "name": "ChatMessage",
  "type": "object",
  "properties": {
    "sender_email": {
      "type": "string"
    },
    "receiver_email": {
      "type": "string"
    },
    "pet_id": {
      "type": "string"
    },
    "content": {
      "type": "string"
    },
    "image_url": {
      "type": "string"
    },
    "is_read": {
      "type": "boolean",
      "default": false
    },
    "chat_room_id": {
      "type": "string",
      "description": "Combination of emails + pet_id for grouping"
    }
  },
  "required": [
    "content",
    "chat_room_id"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "data.sender_email": "{{user.email}}"
        },
        {
          "data.receiver_email": "{{user.email}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.sender_email": "{{user.email}}"
        },
        {
          "data.receiver_email": "{{user.email}}"
        }
      ]
    },
    "delete": {
      "data.sender_email": "{{user.email}}"
    }
  }
}